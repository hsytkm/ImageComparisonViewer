<UserControl x:Class="Control.ExplorerAddressBar.DirectoryPathNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Horizontal" >
        <Button x:Name="DirectorySelectButton"
                Margin="1,0"
                Padding="7,2"
                Background="Transparent"
                Click="DirectorySelectButton_Click" />

        <ComboBox x:Name="ChildDirectoryComboBox"
                  Margin="1,0"
                  Width="18"
                  SelectionChanged="ChildDirectoryComboBox_SelectionChanged" >

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FullName, Mode=OneWay}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>
