<UserControl x:Class="ICV.Control.ExplorerAddressBar.ExplorerAddressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vc="clr-namespace:ICV.Control.ExplorerAddressBar.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="ground"
             Height="20" >

    <UserControl.Resources>
        <ResourceDictionary>
            <vc:InverseVisibilityConverter x:Key="InvVisibilityConverter" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ImageComparisonViewer.Common;component/Themes/DarkPurple.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- ItemsControl攻略 ～ 外観のカスタマイズ http://grabacr.net/archives/1240 -->
    <!-- Standard Controls - ItemsControl http://yujiro15.net/YKSoftware/StandardControls_ItemsControl.html -->

    <!-- MainWindowが小さくなっても本来のサイズを取得するためScrollViewerで包む -->
    <ScrollViewer IsTabStop="False" 
                  VerticalScrollBarVisibility="Hidden"
                  HorizontalScrollBarVisibility="Hidden" >

        <!-- ItemsControlの不使用領域を含ませないためGridで包む -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- ディレクトリノード -->
            <ItemsControl x:Name="NodeItemsControl"
                          Background="{StaticResource BackgroundBrushKey}"
                          Height="{Binding ActualHeight, ElementName=ground, Mode=OneWay}"
                          Visibility="{Binding Visibility,
                                               ElementName=DirectoryPathTextBox,
                                               Converter={StaticResource InvVisibilityConverter}}" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <!-- ノード横の空白Grid(クリック用) -->
            <Border x:Name="GroundMargin"
                    Grid.Column="1"
                    Background="{StaticResource BorderBrushKey}"
                    Visibility="{Binding Visibility, ElementName=NodeItemsControl, Mode=OneWay}"
                    MouseLeftButtonDown="GroundMargin_MouseLeftButtonDown" />

            <!-- 入力用 -->
            <TextBox x:Name="DirectoryPathTextBox"
                     Grid.ColumnSpan="2"
                     VerticalContentAlignment="Center"
                     KeyDown="DirectoryPathTextBox_KeyDown" />

        </Grid>
    </ScrollViewer>

</UserControl>
